import { fromJS } from 'immutable'

const initialState = {
    isLogin:false,
}

export default (state= initialState,action) => {
    switch(action.type) {
        case 'login/LOGIN':
            
    }
}
